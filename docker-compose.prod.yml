services:
  web:
    build:
      context: .
      target: prod
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NEXT_TELEMETRY_DISABLED: "1"
      NODE_ENV: production
    restart: unless-stopped
